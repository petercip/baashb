Hi <%= @member.name.split.first %>,

We received a request to reset your <%= @club.name %> password.

Reset your password:
<%= @url %>

This link expires in 2 hours. If you didn't request a reset, you can safely ignore this email — your password has not changed.

— <%= @club.name %>
